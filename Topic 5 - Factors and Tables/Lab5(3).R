#Q3
chisq.test(c(380, 100, 80, 20), p=c(0.60, 0.20, 0.15, 0.05))

# Based on the results acquired from the goodness-of-fit test, there is
# a statistically significant difference between the distribution of current
# and past causes of road accidents since (p-value = 0.03664) < (a = 0.05).