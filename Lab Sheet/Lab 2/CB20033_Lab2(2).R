#Q2(i)

# x^2 +y^2 +2gx +2fy +h = 0
# A(1,0), B(0.5,0), C(1,-1)

# 2g +h = -1
# g +h = -0.25
# 2g -2f +h = -2

A <- rbind(c(2*1, 2*0, 1), c(2*0.5, 2*0, 1), c(2*1, 2*-1, 1)); A
B <- c(-(1^2 + 0^2), -(0.5^2 + 0^2), -(1^2 + (-1)^2)); B

#Q2(ii)
solve(A) %*% B

# Therefore, g=-0.75, f=0.50, h=0.50
